import { describe, it } from 'vitest';

describe('simulateToCanvas', () => {
  it.skip('renders bullet trails to canvas', () => {
    // TODO: Playwright E2E — needs Canvas2D context
  });

  it.skip('applies normalizer to positions before rendering', () => {
    // TODO: Playwright E2E — needs Canvas2D context
  });

  it.skip('uses configurable colors and background', () => {
    // TODO: Playwright E2E — needs Canvas2D context
  });
});
